# Pazoo：グーネット在庫の日次確認（2026-09-04 社長決定）

社長「ぐーの在庫1日1回確認して出して」／出し先＝**けんしろうの21時LINEに合流**／頻度＝**変わった日だけ知らせる**（確認は毎日）。

## 道具

| ファイル | 何をするか |
|---|---|
| `tools/goo-stock.mjs` | グーネットの在庫一覧を取ってきて読む。`data/` に結果を書く |
| `tools/apply-stock.mjs` | 取ってきた実在庫を `demo/pazoo.html` の `STOCK` に反映する |
| `data/stock_latest.json` | 最新の在庫（機械用）。**次回の差分の基準になるので消さない** |
| `data/在庫_latest.md` | 社長が読む用。台数・前回からの変化・全車の明細 |
| `data/history/stock_YYYY-MM-DD.json` | 日ごとの記録 |
| `data/stock_line.txt` | 21時LINEに足す1行。**変化が無い日は空** |
| `data/goo-stock.log` | 取得に失敗した日の記録 |

## 毎日の流れ（自動）

`HQ_けんしろう夜間タスク表_21時`（Windowsタスクスケジューラ）→ `C:\HQ\kenshiro\taskhyo\nightly.ps1` の **手順5.5** で `goo-stock.mjs --quiet` を実行 → 変化があれば `stock_line.txt` に1行 → `tools/format-line-url.mjs` がLINE本文に足す。

LINEの見え方（変化があった日だけ3行目が出る）：

```
▼ ぶんちゃん 9/5(土)
■ ⚠要返信5 🆕新規9 …（計37人）
■ Pazoo在庫 6台（🆕1 ✅1）
▼ 案件ボード
https://…
```

## 手で回すとき

```
node C:/HQ/projects/pazoo-hp/tools/goo-stock.mjs          # 取ってきて画面に出す
node C:/HQ/projects/pazoo-hp/tools/apply-stock.mjs        # サイトへの反映を下書きだけ見る
node C:/HQ/projects/pazoo-hp/tools/apply-stock.mjs --write # 実際に pazoo.html を書き換える（.bak を残す）
node C:/HQ/projects/pazoo-hp/demo/tools/build_pazoo.js    # 配布版を作り直す → Artifact へ上げる
```

★**Artifact への反映は手作業**。`claude -p`（無人実行）にはArtifactツールが渡らないため、自動では上げられない。

## ハマりどころ（実測で踏んだもの）

- 🔴 **要約AIに読ませてはいけない。** WebFetch は「ジムニー」を「ダイハツ ムーヴ」、75万円を「1,350万円」と読んだ。**必ず生HTMLをパースする**
- 🔴 **グーネットは EUC-JP。** `res.text()` はUTF-8前提なので全部化ける。`new TextDecoder("euc-jp").decode(await res.arrayBuffer())` を使う
- 🔴 **値段はセルの中だけで探す。** 範囲を広げるとローンの「月々 13,500円」を拾う。`要問合せ` は `<em>ASK</em>` / `<em>--</em>` で入る
- 🔴 **表のセルに `<br/>` が入る**（例 `2024<br/>(令和6)年`）。`[^<]*` では取れない
- **車名は上書きしない。** グーの見出しは装備まで入った長文でカードが崩れる。名前とバッジは `pazoo.html` の手書きが正。新しく入った車は `【要確認】` 付きの仮名で入るので、**公開前に必ず人が直す**
- **並び順はジムニー → 旧車 → その他に固定**。グーの並び順のままだとジムニーが分かれ、「ジムニー買うなら、Pazoo.」の店に見えない
- 🔴 **在庫は48時間で自動的に表示が止まる**（`pazoo.html` の安全弁）。2026-09-04 に実際に止まっていた（最終更新2026.09.02＝71時間経過）。**在庫を触ったら必ず `apply-stock.mjs --write` で日付を進める**

## 出典

`https://www.goo-net.com/usedcar_shop/0902905/stock.html`（店舗コード `0902905`。写真URLの `7000902905` とは別番号）
