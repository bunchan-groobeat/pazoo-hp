# Pazoo HP 引継ぎ（2026-09-09）

前の引継ぎ＝`引継ぎ_2026-09-05.md`。
🔴**あれはもう見ないこと。** 「直すのは `demo/pazoo.html` だけ」と書いてあるが、**2026-09-09 に構成が変わって誤りになった。**

---

## 0. 何が変わったのか（これがいちばん大事）

**9月の確定デザインは `demo/pazoo.html`（1枚もののデザイン案）に留まっていて、配信されていなかった。**

| | 2026-09-08まで | 2026-09-09以降 |
|---|---|---|
| 配信されていたトップ | `index.html`＝**2026-07-30のルイージ版**（旧デザイン） | `index.html`＝**9月デザイン**（本番化済み） |
| 9月デザインの置き場 | `demo/pazoo.html`（title「デザイン案」・下層リンク0本） | 役目を終えた。**もう触らない** |
| 下層8ページ | 7月版のクラス体系（base.css / theme.css） | **9月デザインの作りで全部書き直し** |
| CSS・JS | `demo/pazoo.html` にインラインで埋まっていた | `assets/css/pazoo-2609.css`／`assets/js/pazoo-2609.js` |

★社長が9/4に出した指示6件（暗い面のサービス章・ジムニーの回り込み・3本線メニュー等）は、
　**9/8までお客様に見える側には1つも載っていなかった。** 9/9で解消。

## 1. どこを直すか（ここだけ見ればいい）

| やりたいこと | 触るファイル |
|---|---|
| **トップの見た目・文章** | `index.html` |
| **下層8ページ** | `maintenance` / `repair-paint` / `coating` / `order` / `order-insurance` / `companyoutline` / `contact` / `privacy` .html |
| **色・字・余白・部品** | `assets/css/pazoo-2609.css`（631行） |
| **動き・在庫・ドロワー** | `assets/js/pazoo-2609.js`（221行＋STOCK） |
| 口コミの中身 | ★手で書かない。`node tools/build-voices.mjs` で `data/reviews_latest.json` から生成 |
| 在庫の中身 | ★手で書かない。`node tools/apply-stock.mjs --write` |
| スマホ・外出先で見せる | `node tools/bundle-artifact.mjs <出力先>` → Artifactに上げる |

- **Artifact URL（トップの1枚版）**＝ https://claude.ai/code/artifact/1c6ff541-323a-4949-ab86-867cb8752211
- 手元で見る＝ `node <serve> C:/HQ/projects/pazoo-hp 8901` → http://localhost:8901/
  （同じWi-Fiのスマホからは `http://192.168.1.5:8901/`。ポート8899は別サイトが使用中）

★`demo/` はもう成果物ではない。`demo/pazoo.html` `demo/artifact_pazoo.html` `demo/tools/build_pazoo.js` は**過去の記録**。

## 2. 自動で入るもの（手で書くと消える）

### 在庫（グーネット）
```
毎晩21時  nightly.ps1 → goo-stock.mjs        … データを取る（data/stock_latest.json）
毎晩21時  nightly.ps1 → apply-stock.mjs --notify … 反映が要る日だけ社長のLINEに1行
朝        社長が   apply-stock.mjs --write    … サイトに反映（人が確認してから）
```
🔴**在庫は48時間で自動的に表示が止まる**（`pazoo-2609.js` の安全弁）。
　`--notify` が30時間を超えたら「あと約N時間で在庫表示が止まる」とLINEに出す。**出たら朝に `--write` を流す。**
　★自動で書かないのは社長決定「2」（2026-09-09）。**新しく入った車は仮名`【要確認】`になるため、そのまま客に見せられない。**

### 口コミ（グーネット）
`goo-reviews.mjs` が取る → `node tools/build-voices.mjs` で全10件を生成。
🔴**自己申告の `aggregateRating`（星の平均を構造化データで書く）は禁止**（CHECKLIST_KOKAI D分野）。
　画面の数字は「グーネットの集計値の引用」＋出典リンクという形を崩さない。

## 3. いま残っている宿題

### 🔴 公開前に必須（埋まらないと機能しない）
| # | 何が | いまどうなっているか |
|---|---|---|
| 1 | **FormspreeのフォームID** | `formspree.io/f/YOUR_FORM_ID` のまま＝**送信しても誰にも届かない** |
| 2 | **LINE公式アカウントのURL** | 全9ページの「LINEで相談」が `#`（押しても何も起きない） |
| 3 | **InstagramのFeed ID** | トップのInstagram欄が空（Behold.so） |

### 要確認25件・素材待ち9枠
一覧＝`確認シート_2026-09-09.md`。**推測で埋めていないので、サイトに「要確認」の印が出たまま。公開前に全部消す。**

### 入れ替え（pazoo.co.jp への差し替え）
手順の正本＝`C:/HQ/PLANS/リプレース手順_3サイト_2026-09-09.md`。
- 301表＝`_301_本番用.htaccess.txt`（14本・旧11URLを実測して作成・未カバー0件）
- 本番robots＝`_robots_本番用.txt`（AI検索クローラ6種を許可）
- URL置換＝`_shared/set-site-url.sh`（既定dry-run。canonical等76箇所がまだ github.io を指している）
- ⬜ 社長承認待ち＝`/used-car/` の行き先（暫定 `/order.html`）

## 4. 踏んだ罠（同じ穴に落ちないため）

1. **🔴「無い」をgrep1回で判断しない。**
   太郎は「goo-net口コミはHTMLに1件も無い」と報告したが**誤り**だった。`口コミ` で検索したが、
   実際の表記は「お客様の声／Reviews／レビュー総合」。**言い換えを2〜3通り試すまで「無い」と言わない。**
2. **🔴 自動処理の書き込み先が納品物とは限らない。**
   `apply-stock.mjs` は `demo/pazoo.html` に書き続けていた。毎晩データを取っても**サイトには永久に反映されない**状態だった。
   **構成を変えたら、その構成に書き込む自動処理を全部洗う。**
3. **🔴 通知は受け手まで実測する。**
   `format-line-url.mjs` が `stock_line.txt` の**1行目しか読まない**作りで、足した警告が黙って捨てられるところだった。
4. **JSはトップ専用の要素を触る。** `.hero` 等は下層に無い。ガードを入れないと `TypeError` で**そのページのJSが全部止まる**。
5. **Bash経由でバックスラッシュが落ちる。** `\tools\apply` がタブ＋ベル文字に化けた。
   **Windowsパスを含む書き換えは Write/Edit かスクリプトファイルで。** 書いたあと必ず実体を見る。

## 5. この日のcommit

| リポジトリ | commit | 中身 |
|---|---|---|
| pazoo-hp | `c722fa7` | 入れ替え準備（noindex・llms.txt・301表・本番robots） |
| pazoo-hp | `1c249a5` | **9月デザインを本番9ページへ実装** |
| pazoo-hp | `00a0bab` | goo評価を全10件に＋1枚版の生成ツール |
| pazoo-hp | `ef3b713` | goo在庫を反映（4台）＋反映先を納品物へ向け直す |
| pazoo-hp | `bbbbf85` | 在庫を夜間下書き＋LINE通知に／紹介文を修正 |
| HQ | `780b6a8` | nightly.ps1 に反映確認を追加／LINE整形が1行目しか読まない問題を是正 |

★**pushは未実施**（社長が `!` で流す）。
