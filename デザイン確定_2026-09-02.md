# Pazoo HPリニューアル — デザイン案の到達点（2026-09-02 保存）

社長「一旦これで保存しておいてくれ」。**工程3（デザイン）の途中経過をここで確定させる。**
工程の正本＝`C:\HQ\PLANS\HP制作パイプラインv2.3.md`。案件の正本＝`REQUIREMENTS.md`。

---

## 1. 成果物

| ファイル | 用途 |
|---|---|
| **`demo/pazoo.html`** | ★**これが現物**。PCのブラウザで直接開く（`★Pazoo案を見る.bat`） |
| `demo/artifact_pazoo.html` | 上から自動生成した配布用（画像を埋め込んだもの）。編集しない |
| **Artifact URL** | https://claude.ai/code/artifact/9713878a-f2c6-4b50-aa17-b7c0c57b4558 （スマホ用・同じ中身） |
| `demo/_旧3案/` | 9/2午前の3案比較ページ。**役目は終わり**。参照のみ |

★**直すのは `pazoo.html` だけ。** 直したら `artifact_pazoo.html` を生成し直してArtifactへ上げる。

---

## 2. 決まったこと

### 方向
- 🔴 **中古車販売がメイン**（社長 2026-09-02）。板金・保険を主役にしていた設計は誤りだった
- **キャッチ＝「ジムニー買うなら、Pazoo。」**（在庫6台のうち3台がジムニー＋アメ車のグランドワゴニア＝実態と一致）
- **参考＝Renoca**（https://www.flexnet.co.jp/renoca/ ）。社長が選定
- **作り直し**（差分改修ではない）。ただし旧サイトのSEO資産（JSON-LD・NAP・sitemap・webp化）は移植する

### 見た目
| 項目 | 決定 |
|---|---|
| 地 | **クリーム `#F4F0E8`**。無彩色で組み、色は写真とロゴの2色だけ |
| ロゴの色 | ワイン `#7D2A3D` ／ ミント `#5FC9A6` |
| 暗い面 | **「事故にあわれたら」の章だけ**チャコール `#211F1B` に沈める（コントラストを作る） |
| 書体 | **ゴシック**（社長指定）。和文＝Noto Sans JP ／ 英字＝Barlow Condensed |
| 見出し | **英字を最大104pxで大きく**、和文を小さく添える（Renoca型） |
| トップ | 写真4枚のスライド。**左25%に透明度72%の透かしパネル**（上にロゴ・下に文言） |
| ヘッダー | **LINEと電話**（フォームは作らない＝ぶんちゃんHPと同じ考え） |

★**没にしたもの**＝蛍光ミントの多用（安く見える）／クリームと蛍光の同居（濁る）／白地カタログ型（在庫6台では台数で負ける）

### 動き（Renocaのソースから数値ごと再現）
出典＝`style.css` / `index.css` / `common.js` / `index.js`（GSAP TweenMax を使用）

| 動き | 数値 |
|---|---|
| 見出しのワイプ | 帯が左→右へ `transform 1.2s cubic-bezier(.66,.45,.14,.79)`／文字は0.5s遅れて表示 |
| 持ち上がり | `translateY(50px)` → `.5s cubic-bezier(.215,.61,.355,1)` |
| 静かに現れる | `opacity .8s` |
| 発火の条件 | **(スクロール量＋画面高) > 要素の上端 ＋ 画面高×0.20** |
| スライド | **進捗バーが5秒・等速で満ちたら次へ**（点ではなくバー） |
| ヒーロー | 写真が6.5秒かけて 1.04倍→等倍 |

★**`prefers-reduced-motion: reduce` で全部止まる。** Windowsの「アニメーション効果」がオフだと動かないのは仕様。

---

## 3. 在庫（実データ）

2026-09-02 時点のグーネット掲載＝**6台**（10台ではない）。店主様の許諾は取得済み（社長申告）。

| 車名 | 年式 | 走行 | 支払総額 |
|---|---|---|---|
| ジムニー ランドベンチャー | 1997 | 20.3万km | 75万 ★**修復歴あり** |
| ジムニー XL | 1997 | 14.6万km | 103万 |
| ジムニー XC | 2024 | 1.1万km | **応談** |
| ジープ・グランドワゴニア | 1990 | 走行不明 | 407.5万 |
| スペーシア ハイブリッドX | 2020 | 9.7万km | 75万 |
| キャリイトラック | 2015 | 2.2万km | 77万 |

- カードを押すと**グーネットの車両ページ**が別タブで開く
- **最終更新から48時間で在庫欄が自動的に隠れる**（売約済みが残る事故を防ぐ）
- 🔴**写真はいまグーネットの画像を参照している。本番までに元写真を自社サーバーへ移す**
- 🔴 グーネットのHTMLを機械的に取り込む・枠内に埋め込むのは**利用規約で禁止**。やらない

---

## 4. 残っている宿題

### 社長が動けば進むもの
1. **確認シート（8問）を店主様へ送る**＝`確認シート_LINE用_2026-09-02.md`。特に**料金**と**工事中3ページの中身**がないと先へ進めない
2. **LINE公式アカウントのURL**（調査では見つからず。無ければ作るところから）
3. **在庫データの受け取り方**＝MOTOR GATEからCSV等で出せるかをプロトの担当者に確認
4. **GBPのオーナー権限**（HPと同時に整備する方針）

### 太郎の宿題
- 🔴 **写真の撮り直し**。Renoca型は**屋外・自然光の写真で成立する型**。いまあるのは2020年撮影の4枚だけで、**骨格は真似られても空気は写真でしか作れない**
- 工程5（SEO・AIO）＝llms.txt・JSON-LD・sitemap。**旧サイトから移植**
- 工程6（検品）＝`CHECKLIST_KOKAI.md`
- 会社概要の数字（スタッフ数・創業年）は**2020年のままの可能性が高い。転記しない**

---

## 5. リスク（3:1）

- 🔴 **この案の完成度は写真で決まる。** Renocaが成立しているのは屋外・自然光のヴィンテージ写真があるから。現有4枚のままだと「整った案」止まりで、社長が求めた「かっこいい」には届かない
- **在庫6台は少ない。** 横スクロールが2画面で終わるので、台数が減るとスカスカに見える。3台を切ったら並べ方を変える必要がある
- **書体の質感は再現できていない。** Renocaはモリサワ（TypeSquare）とTypekitの有料フォント。Google Fontsでは線が違う。本番で有料書体を使うかは別途判断

---

## 6. 崩し3点と F 自己チェック（2026-09-03 追記・AI臭回避の組み込み＝`CHECKLIST_KOKAI.md` F軸）

社長「PazooHPに今のスキル反映させて」。工程3の必須物＝**崩し3点**をここに固定する（これが無いカンプは工程4に渡さない）。

### 崩し3点（定番からどう外すか）
| 軸 | 定番（やらない） | Pazooの外し方 |
|---|---|---|
| 配色 | 車屋の定番＝紺×銀・赤×黒、業種色のグラデ、角丸＋影のボタン | クリーム `#F4F0E8` の地にロゴ2色（ワイン・ミント）だけ。暗い面は「事故にあわれたら」の1章だけ。グラデはヒーロー写真の可読性用1本のみ（装飾に使わない）。ボタンは角丸2px・影なし |
| 書体 | Noto Sans 一本で大小を付けるだけ | 英字 Barlow Condensed（大見出し・数字・価格）と和文 Noto Sans JP（本文）の役割分担。見出しは英字最大104px・和文14pxを添える |
| レイアウト | 同サイズのカード3〜4列・全部中央揃え・全セクション同じ高さ | サービス03〜06を**台帳の行**にし、差別化の2行（板金・保険）だけ幅と高さを変える。声の引用は左18%ずらして大きく。連絡先は3:2の非対称。中央揃えはヒーローの1か所だけ。声の章だけ余白を変える |

### F 自己チェック（`demo/pazoo.html`・2026-09-03）
| # | 項目 | 判定 | 根拠・処置 |
|---|---|---|---|
| 1 | 文章の型 | 🟢 | 「お届け／安心／信頼／丁寧」0件（grep）。固有名詞と数字＝須賀川市北山寺町・国道4号・IC5分・JAAA・認証工場4-7761・3等級 |
| 2 | 同サイズのカード3列 | 🟢（直した） | 03〜06の4等分グリッドを台帳の行に置換。汎用アイコン0 |
| 3 | 絵文字・✓ | 🟢 | 0件（grep） |
| 4 | 定番配色のグラデ・角丸影ボタン | 🟢 | gradient 1（写真可読性用）・box-shadow はカードのhoverのみ・border-radius 2px |
| 5 | 書体1本 | 🟢 | 2書体・役割分担 |
| 6 | 均一整列 | 🟢（直した） | 声の左18%オフセット・連絡先3:2・声の章の余白変更 |
| 7 | 実写真 | ⏭ 育成 | 2020年撮影4枚＋在庫はグーネット写真。**店の前のジムニー**を撮り直す（既知・確認シート⑩で依頼） |
| 8 | 語り手 | ⏭ 育成（枠は新設） | 冒頭に「店主のひとこと」枠を新設し**素材待ち**を明示。原文が届き次第そのまま載せる（確認シート⑨） |

→ 設計由来の6点はすべて🟢。素材由来の2点は**未着＝育成リスト**（減点しない）。届いたのに未掲載なら🟡になる。

### 2026-09-03 社長指示の反映（「サービスの背景、黒っぽい方にして。小さい文字は大きく」）
- **暗い面は2章**＝Service と Insurance（§2「暗い面は事故の章だけ」を変更）。連続する暗い章の境目は1px線 `#3b372f`。
- **文字を大きく**＝lead 15→16px／カード本文 14→16px・見出し17→19px／台帳の本文 14→16px・見出し18→20px・英字添え 11→12.5px・右側の具体 13.5→15px（和文書体に統一）／番号 12→13px。
- 暗い面での「要確認」印はミント色に（ワイン色は暗い地で見えない）。
- 実測＝playwright 390px で横はみ出し0／PC 1280px のスクショで目視。Artifact 更新（版「サービス章を暗い面に 9/3」）。

### 2026-09-04 社長指示の反映（4件）
| 指示 | 実装 |
|---|---|
| 「文字が流れるアクションもっとゆっくり」 | 見出しのワイプ＝帯 1.2s→**2.2s**、文字の出現 .5s→.95s（w1 は帯 .3s／文字 1.25s 遅れ） |
| 「フッターにもロゴ入れて」 | フッター左上にロゴ（高さ64px・スマホ48px）。ロゴはクリーム地用の色なので**クリームの札**に載せる（暗い面でワインが沈まないため） |
| 「ヘッダーメニュー3本線」 | 文字ナビが隠れる **1040px以下**で3本線ボタン → 全画面の暗いメニュー（7項目＋LINE・電話）。Esc／リンク押下／PC幅に戻すと閉じる。★drawer はヘッダーの**外**に置く（sticky＋backdrop-filter の中だと position:fixed の基準がヘッダーになって出ない＝実測で踏んだ） |
| 「ジムニー買うなら一回り大きく」 | `.l1` clamp(22px,4.4vw,66px)→**clamp(27px,5.6vw,84px)**、スマホ 21→26px |
| 「写真がかわったら、差し色だけ違う色に。全て蛍光色」 | `--accent` を写真ごとに JS で切替＝**#3dffa0（蛍光ミント）／#f6ff00（蛍光イエロー）／#ff2bd6（蛍光ピンク）／#00f0ff（蛍光シアン）**。差し色＝「ジムニー買うなら、」・Pazoo の「.」・進捗バー。★§2「色はロゴの2色だけ」の**例外**（社長指示） |
| 「最新画面表示したら、ゆっくり文字がうきでてくる。画面変わったらまた、ゆっくり『ジムニー買うなら』からタイピングで表示」 | 初回＝1.8sで浮き出る（.l1 .5s遅れ／.l2 1.1s遅れ）。写真が変わるたび＝`.l1` を消して **140ms/字のタイピング**（蛍光カーソル点滅）→ 打ち終わったら `Pazoo.` が1.6sで浮く。5秒の切替内に収まる（約3.2s）。reduced-motion では全部即表示 |

実測（playwright 390px）＝タイピング途中「ジムニー」→完了「ジムニー買うなら、」／2枚目の差し色 #f6ff00／メニュー全画面 390×844・リンク押下で閉じてスクロール／PC 1280px では3本線が出ず文字ナビ表示／JSエラー0／横はみ出し0。Artifact 更新（版「3本線・タイピング・蛍光差し色 9/4」）。

### 2026-09-04 訂正（社長「1色 ミントだけで大丈夫！タイピングも最初だけ」）
- **差し色は蛍光ミント `#3dffa0` の1色に固定**（写真ごとの4色切替は取りやめ。同日の「全て蛍光色」を変更）。
- **タイピングは最初の表示の1回だけ**。写真が切り替わっても文字はそのまま（切替時の打ち直しは取りやめ）。
- 実測（playwright 390px）＝初回 1.3s時点「ジムニー買う」→完了「ジムニー買うなら、」・Pazoo. 表示／2枚目へ切替後も文字不変・差し色 #3dffa0／メニュー・JSエラー0 変わらず。Artifact 更新（版「ミント1色・初回のみタイピング 9/4」）。

### 2026-09-04 追加写真5枚の反映（社長支給）
素材＝`素材/追加写真_2026-09-04/`（原本5枚＋切り抜き1点）。**確認シート⑩の写真がこれで一部埋まった**（🔴「ジムニーを主役にした写真が要る」が解消）。

| 何を | どうした |
|---|---|
| **トップの写真スライド**（社長「新2枚＋既存2枚を混ぜる」） | 1枚目＝`mv-jb64-front.webp`（店の前のJB64・**店の看板が写る**）／2枚目＝`mv1.webp`（既存・店舗外観）／3枚目＝`mv-jb64-rear.webp`（JB64のリア）／4枚目＝`mv3.webp`（既存・待合）。★4:3の写真をPCの21:9枠に入れると看板が切れるため、1枚目だけ `object-position:center 13%` で上寄せ（実測で決めた値） |
| **FV直下の帯**（社長「赤のジムニー切り取りして使いたい。背景抜いて」） | `.intro` を2段組みにし、右に**切り抜いた赤（ワイン）のジムニー**を置いて画面右端へ逃がす（`margin-right:min(0px,calc((var(--max) - 100vw)/2))`＋`.intro{overflow-x:clip}` で横スクロールを出さない）。左に「いまある車を見る →」を追加。900px以下は縦積み |
| **SERVICE の見出し**（社長「SERVICE ミント色に」） | `#service .h2{color:var(--mint)}`。和文の添え字（買う。探す。直す。守る。）は `#b3ada2` のまま |

**切り抜きの作り方（再現手順）**＝Photoshop 2026 をJSXで叩く。①被写体を選択（`executeAction(stringIDToTypeID("autoCutout"))`）②選択を1px内側＋0.5pxぼかし ③反転して削除 ④透明部分をトリミング ⑤PNG保存。**Select Subject は車の上に写り込んだ日除けの梁と支柱まで拾う**ので、2回目のJSXで車の輪郭より上を通る多角形を選んで消した。★イラレ同様、**社長と同時にPhotoshopは使えない**。走らせる前に `Get-Process Photoshop` で起動していないことを確認する。

**まだ埋まっていない素材**
- ⑨店主のひとこと＝**素材待ち**（枠はそのまま表示中）
- ⑩A「人（店主様かスタッフ）」＝**未着**。今回の5枚は全部「場所」と「物」
- ⑪数字1つ＝**未着**
- 切り抜きの下に付けた `要確認 車種名・年式` ＝**車種を推測で書かない**ため空にしてある

実測（Chromeヘッドレス）＝PC 1440px／スマホ幅（Chromeの最小ウィンドウ幅は500pxのため500pxで確認）。`documentElement.scrollWidth = innerWidth`＝**横スクロール0**。1枚目のヒーローで店の看板が写ることを目視。SERVICE がミントで出ることを目視。Artifact 更新（版「追加写真＋赤ジムニー切り抜き 9/4」）。

### 2026-09-04 トップ写真の選定と、スマホの切り抜き配置（社長）
- 候補を**PCのトップ枠（21:9）に実際の文字を載せた9案**で提示 → 社長選定＝**1・2・6・7・8・9**（比較ページ＝`demo/候補_トップ写真.html`）。
- **トップは6枚**：①`mv-jb64-front`（店の看板が写る／`object-position:center 13%`）→ ②`mv-ja-red`（赤・ローアングル／`center 32%`）→ ③`mv1` → ④`mv2` → ⑤`mv3` → ⑥`mv4`。進捗バーも6本に。
- 🟡 **⑥ `mv4` は 950×300 と小さい**（他は1477〜1600px）。PCの1600px級の枠では引き伸ばしになる。**元写真をもらえれば差し替える**（社長は承知の上で選定）。
- 🟡 **②の赤ジムニーは、FV直下の帯の切り抜きと同じ車**。同じ一台が2回出る。気になるようなら②を外すか、帯を別の車にする。
- **スマホ（900px以下）の切り抜きの置き場所**（社長「スマホ版ここにジムニー入れて」＝見出し右の空きを丸で指定）＝`.intro-car` を `position:absolute; top:-34px; right:-20px; width:min(50%,190px)` で**見出しの右へ絶対配置**し、画面の右端まで出す。★実測（390px）＝見出し2行 87px＋余白26px／`CLASSIC 4WD` の行幅194px。この寸法だと文字にかからず、本文にも重ならない。
  - スマホでは `.intro-car .cap`（`要確認 車種名・年式`）を**非表示**。幅が足りず2行になって本文にかぶるため。**PCでは出る**。

実測＝6枚が front → 赤 → mv1 → mv2 → mv3 → mv4 → 先頭 と5秒ごとに循環（進捗バーは常に1本だけ点灯）／JSエラー0／`scrollWidth = innerWidth`＝横スクロール0／スマホ390pxはiframeで再現して目視（Chromeの最小ウィンドウ幅が500pxのため）。Artifact 更新（版「トップ6枚・スマホ見出し右に車 9/4」）。

### 2026-09-04 訂正（社長「トップ画像赤ジムニー外して」）
- **トップは5枚**＝①`mv-jb64-front`（看板入り）→ ②`mv1` → ③`mv2` → ④`mv3` → ⑤`mv4`。進捗バーも5本。同日の選定「1・2・6・7・8・9」のうち**2（赤ジムニー）を取りやめ**。
- 理由＝**FV直下の帯の切り抜きと同じ一台**で、同じ車が2回出ていたため。赤ジムニーは**帯の切り抜きとしてだけ**使う。
- `mv-ja-red.webp` と `.hero .slide.p-red` の指定は削除。素材は `素材/追加写真_2026-09-04/01_ジムニー旧型ワイン_ローアングル.jpg` に残す。
- 配布版は 2.14MB → **1.73MB**（参照されない画像は埋め込まれないため）。

実測＝front → mv1 → mv2 → mv3 → mv4 → 先頭 と5秒ごとに循環／進捗バー5本・常に1本だけ点灯／JSエラー0／横スクロール0。Artifact 更新（版「トップ5枚（赤ジムニーを外した） 9/4」）。

### 2026-09-04 追加の3件（社長）
| 指示 | 実装 |
|---|---|
| 「ジムニーの切り抜きもサイドから回って登場アクション入れて」 | `.ani_drivein` を新設。`translateX(62%) rotate(-9deg) scale(.92)`（起点＝後輪あたり `22% 92%`）→ 等倍・水平へ。**transform 1.6s** `cubic-bezier(.17,.78,.28,1)`・opacity 1s・どちらも0.15s遅れ。フロントを持ち上げた姿勢で右の外から入り、走り込んで落ち着く |
| 「在庫自動でスライド流れてる感じ」 | 在庫レールを**切れ目なく自動で流す**。同じ並びを**2組**置き（2組目は `aria-hidden` ＋ `tabIndex=-1`）、半分まで進んだら半分ぶん戻す。**約27px/秒**。触っている間・タブが隠れている間は止まり、←→を押すと1.6秒待ってから再開。`prefers-reduced-motion` では流さない。吸い付き（`scroll-snap-type`）は流れを引き戻すので `none` にする |
| 「マル枠ない外し」 | `.banner`（「これは社内確認用のデザイン案です…」の枠）を**削除**。`noindex` は `<head>` にあるので検索には出ない。`要確認` の印そのものは残す |

🔴 **実測で踏んだ罠2つ**
1. **`rail.scrollLeft += 0.45` は永久に進まない。** 小数が整数に丸められて毎フレーム同じ値に戻る。→ **位置はJS側の変数 `pos` で持ち**、`rail.scrollLeft = pos` と代入する形に直した。
2. **Chromeヘッドレスでは `requestAnimationFrame` が数フレームで止まる**（`--virtual-time-budget` を付けても9回で停止）。動きの検証は **rAFを `setTimeout` に差し替えて**ロジックを測るか、実ブラウザで見る。

実測＝流れる（L14→36→64）／末尾から先頭へ切れ目なく折り返す（L2300→L2→11→31）／触ると止まる（L36で固定）／カード12枚＝6台×2組／`scroll-snap-type:none`／JSエラー0／横スクロール0（PC1440・スマホ幅とも）。登場アクションは120ms・400ms・800msのコマで、右外から傾いて入り水平に落ち着くのを目視。Artifact 更新（版「在庫の自動スライド／注記の枠を外した 9/4」）。

### 2026-09-04 在庫スライドを速く（社長「在庫スライドもっともっと早く」）
- **27px/秒 → 130px/秒**（約4.8倍）。カード1枚（386px）を**約3.0秒**で通り過ぎ、6台を一巡するのに約18秒。
- 🔴 **同時に「1フレームいくつ」方式をやめ、経過時間で進める方式に変えた。** 前の作りは60Hz前提で、**120Hzの端末では2倍速**になっていた。`pos += SPEED * dt / 1000`（`dt` は1回100msで頭打ち＝別タブから戻った瞬間に飛ばないため）。
- 速さを変えたい時に触るのは `demo/pazoo.html` の **`var SPEED = 130;`** の1か所だけ（単位＝px/秒）。

実測＝**130px/秒ちょうど**（260px / 2.00秒）／触ると止まる（L134で固定・0.8秒後も134）／離すと再開（0.8秒で238＝130px/秒）／JSエラー0。Artifact 更新（版「在庫スライドを130px/秒に 9/4」）。

### 2026-09-04 訂正2件（社長）
| 指示 | 実装 |
|---|---|
| 「01 — Order これ全部文字大きく色ミント」 | `.cell .no`（01・02）と `.ledger .no`（03〜06）を **13px → 19px**。★**01・02はミントが効いていなかった**＝`<p class="no">` なので `.dark .cell p{color:#b3ada2}`（灰色）に負けていた。`.dark .cell .no{color:var(--mint)}` を足して解消。**社長の指摘どおりの実バグだった**（実測：修正前 `rgb(179,173,162)` → 修正後 `rgb(95,201,166)`） |
| 「在庫自動スライド無し」 | 自動で流す仕掛けを**全部撤去**。同日に入れた2組ぶんのカード複製・rAFループ・一時停止の仕掛け・`scrollSnapType='none'` をやめ、**カードは1組・吸い付き（scroll-snap）を復活**。動かすのは指のスワイプ・←→ボタン・キーボードのみ |

実測＝カード6枚（複製なし）／勝手に動かない／`scroll-snap-type: x mandatory` に復帰／`.cell .no` `.ledger .no` とも 19px・`rgb(95,201,166)`／JSエラー0。Artifact 更新（版「番号をミントで大きく／在庫の自動スライド廃止 9/4」）。

★同日の「在庫自動でスライド流れてる感じ」「もっともっと早く」は**取りやめ**（記録として残す）。130px/秒・時間ベース進行・切れ目のない折り返しまで作って実測済みだったので、また要るときは git 履歴か本ファイルの前節を見れば戻せる。

### 2026-09-05 INSURANCE もミントに（社長「INSURANCE ミント色」）
- `#service .h2` に `#insurance .h2` を並べた（`#service .h2,#insurance .h2{color:var(--mint)}`）。**暗い面の大見出し2つがミントで揃った**。
- 和文の添え字「事故にあわれたら」は `#b3ada2` のまま（SERVICE と同じ扱い）。

実測＝`#insurance .h2` = `rgb(95,201,166)`／`#service .h2` = 同じ／和文 = `rgb(179,173,162)`／JSエラー0。Artifact 更新（版「INSURANCEもミントに 9/5」）。

### 2026-09-05 見出しの帯を残す（社長「STOCK などの帯をそのままはじからはじでのこして白文字」）
いままでは帯が左から右へ抜けて**消えて**いた。明るい面の大見出しは**帯を残す**形に変更。

- 対象＝**明るい面の5つ**（STOCK / VOICE / FAQ / CONTACT / COMPANY）。`section:not(.dark) .h2`。
- 帯＝ワイン `#7d2a3d` を `left:50%;width:100vw;margin-left:-50vw` で**画面の端から端まで**。高さは英字1行ぶん（`top:-.10em;height:1.18em`）で、和文の添え字にはかからない。
- 文字＝**白**。抜けていく帯（`.ani_txt::after`）は明るい面では出さない。代わりに帯そのものが `scaleX(0)→1` で左から伸びる（2.2s・従来のワイプと同じイージング）。
- 暗い面（SERVICE / INSURANCE）は**ミント文字のまま**。帯は付けない。
- `.intro` の「JIMNY & CLASSIC 4WD」は `.big` であって `.h2` ではないので、従来のワイプのまま。

🔴 **踏んだ罠：`#stock` だけ帯が途中で切れていた。**
`left:50%;margin-left:-50vw` は「その要素の中心＝画面の中心」でないと端まで届かない。`.stock-head` が `display:flex` で、見出しが**横並びの狭い箱の中**にあったため中心がズレていた。
→ `.stock-head` を `position:relative` の**縦積み**に変え、「UPDATED …」は `position:absolute` で右下へ置いた（見た目の位置は従来どおり）。560px以下では絶対配置をやめて右寄せの別行にする。

実測＝STOCK/VOICE/CONTACT/FAQ すべて帯が左右の端に到達（PC1440）／スマホ390pxでも端まで到達し「UPDATED」と重ならない／`scrollWidth = innerWidth`＝横スクロール0／JSエラー0。Artifact 更新（版「見出しの帯を端から端まで・白文字 9/5」）。

### 2026-09-05 フッターを作り直した（社長「ヘッター、フッターのロゴ背景透過して」「フッターはんこ屋ぶんちゃんみたいに項目入れて」）

**ロゴの背景**
- **ヘッダーは元から透過だった**（ロゴPNGは RGBA・四隅α=0＝実測）。box は付いていない。**直したのはフッターだけ**。
- フッターは `background:var(--bg)` のクリームの台紙に載せていた（2026-09-02「暗い面ではワインが沈む」という判断）。→ **台紙を撤去**。
- ★**当時の判断は誤りだった。** ロゴは**ミントの縁取り**があるので、暗い地（`#211f1b`）でむしろよく映える（合成して実測）。

**フッターの中身**（`hanko-bunchan/hp/index.html` の `footer` を型にした・4列）
| 列 | 中身 |
|---|---|
| ブランド | ロゴ＋説明文 |
| Menu | いまある車／サービス／事故にあわれたら／お客様の声／よくあるご質問／お問い合わせ／会社概要（7項目・ページ内リンク） |
| Service | 中古車販売／買取・下取り／注文販売／車検・整備／板金・塗装／コーティング／自動車保険（7項目） |
| Shop | 株式会社FIFTY'S／〒962-0061 福島県須賀川市北山寺町318／国道4号沿い・須賀川IC5分／0248-76-2077（FAX 0248-94-5080）／10:00〜19:00 火曜定休／東北運輸局長 認証工場 4-7761／Instagram・LINE |
| 下段 | © Pazoo CAR SHOP／株式会社FIFTY'S ＋ プライバシーポリシー |

- **中身は全部、会社概要の表から取った実データ**。メールアドレスは分からないので**載せていない**（作らない）。
- **LINEのURLは未確認**なので `要確認` の印を付けたまま出している（ヘッダーの「LINEで相談」も同じく `href="#"`）。
- `.ft .todo` を **ミント**にした。フッターも暗い面なので、ワインの印は読めなかった。
- 崩れ幅＝900px以下で2列（ブランドと店の情報は横いっぱい）、560px以下で下段を縦積み。
- 🟡 **プライバシーポリシーのリンク先は `../privacy.html`。** ローカルのデモでは開くが、**Artifact では開かない**（1枚ものなので）。本番でパスを直す。

実測＝フッターの高さ 456px（PC1440）／ロゴの背景 `rgba(0,0,0,0)`＝透過／リンク18本／`要確認` の色 `rgb(95,201,166)`／JSエラー0／横スクロール0／スマホ390pxで2列に畳まれるのを目視。Artifact 更新（版「フッターを4列に・ロゴ台紙を撤去 9/5」）。

### 2026-09-05 在庫に「→」を出す（社長「ストック在庫画像にサイドにスライドさせる→入れて、点滅している」）
- 在庫レールを `.railbox` で囲い、**右端の写真の上に「→」の丸**を重ねた（54px・スマホ44px／`rgba(33,31,27,.72)` の丸に白の矢印）。
- **点滅＋横に動く**（`railhint` 1.5s 無限。opacity 1→.22、同時に `translateX(0→7px)`）。**ただ点滅させるより「横へ動かせる」ことが伝わる**ため動きを足した。
- **押すものではない**ので `pointer-events:none`。写真のリンクを邪魔しない。
- **右端まで送ったら消える**（`scrollWidth - clientWidth - scrollLeft > 8` を `scroll`・`resize`・`load` で判定）。戻すとまた出る。
- `prefers-reduced-motion` では点滅しない（矢印は出る）。
- ←→ボタン（`.railctl`）はそのまま残す。

🔴 **実測の注意：Chromeヘッドレスは `scrollLeft` を代入しても `scroll` イベントを出さないことがある。**
そのせいで「右端に送ったのに矢印が消えない」と誤検知した。`dispatchEvent(new Event("scroll"))` を自分で出して確かめたところ、**残り0で off=true／先頭に戻すと off=false** と正しく動いていた。

実測＝丸54×54・`rgba(33,31,27,0.72)`・文字色 `rgb(255,255,255)`・`z-index:3`／アニメ `railhint 1.5s`／JSエラー0。アニメを止めた状態で等倍撮影し、暗い丸に白い矢印が写真の上に出ることを目視。Artifact 更新（版「在庫に点滅する→を追加 9/5」）。

### 2026-09-05 暗い面も帯＋白文字に（社長「INSURANCEなど黒背景も帯だして、白文字にしてみて」）
- 帯の指定から `:not(.dark)` を外し、**全7セクションの大見出しを同じ形に統一**（STOCK / SERVICE / INSURANCE / VOICE / FAQ / CONTACT / COMPANY）。
- 帯＝**ワイン `#7d2a3d`**・文字＝**白**。暗い地 `#211f1b` の上でもワインははっきり分かれる。
- ★**9/4に入れた「SERVICE・INSURANCE の見出しをミント文字」は取りやめ**（この帯に置き換え）。ミントは `01 — Order` などの通し番号と `.todo` の印に残っている。
- 🟡 **帯をミントにする案は採らなかった。** ミント `#5fc9a6` は明るいので、社長の指示どおり白文字を載せるとほぼ読めない（コントラスト約1.9:1）。ミントの帯にするなら文字は濃い色にする必要がある。

実測＝`#service .h2` の文字色 `rgb(255,255,255)`／7セクションすべてで帯が左右の端に到達／JSエラー0／横スクロール0。Artifact 更新（版「暗い面の見出しも帯＋白文字に 9/5」）。

### 2026-09-05 「いまある車」を外す／暗い面の帯をミントに（社長）
| 指示 | 実装 |
|---|---|
| 「いまある車 外し」 | STOCK の**和文の添え字**（`<span class="ja">いまある車</span>`）を削除。★同じ言葉はドロワー（`Stock<small>いまある車</small>`）・FV直下の「いまある車を見る →」・フッターMenu にも残っている（今回は**外していない**） |
| 「Service 背景黒のとこ帯ミント色」 | `.dark .h2::before{background:var(--mint)}`。**文字は地の黒 `#211f1b`**（`.dark .h2{color:var(--dark)}`）。★社長の以前の指示は「白文字」だが、**ミントに白は約1.9:1でほぼ読めない**。黒にすると約8.6:1。前回この条件を伝えたうえでのご指示なので黒にした |

🔴 **踏んだ罠：「いまある車」を消したら `UPDATED 2026.09.04` が帯の上に乗って読めなくなった。**
`.updated` は `.stock-head` の**右下**に絶対配置していたが、添え字が消えて箱が縮み、右下＝帯の中になったため。→ **`top:1px` に変えて「IN STOCK」の行の右**（帯の外・クリーム地）へ移した。560px以下は従来どおり帯の下に右寄せで置く。

実測＝`#service .h2` 文字 `rgb(33,31,27)`／帯 `rgb(95,201,166)`／`#stock` は文字 `rgb(255,255,255)`・帯 `rgb(125,42,61)` のまま／JSエラー0。PC1440とスマホ390pxで `UPDATED` が帯にかからないことを目視。Artifact 更新（版「暗い面の帯をミントに／いまある車を外した 9/5」）。

### 2026-09-05 ヴィンテージ感／ロゴ横の店名／ロゴ拡大（社長・3件）

**① 「全背景 少しヴィンテージ感だして色のイメージはこのままで」**
- `body::before`＝**周辺の落ち（ビネット）**。`radial-gradient(...rgba(33,31,27,.11))` の黒の透明度だけ。色は足さない。
- `body::after`＝**紙の粒子**。SVGの `feTurbulence`（`baseFrequency .85`／4オクターブ）を `feColorMatrix saturate 0` で灰色にし、220pxのタイルで `opacity:.085`。**画像ファイルは増やさない**（データURI）。
- どちらも `position:fixed` ＋ `pointer-events:none`。操作の邪魔をしない。
- 🔴 **最初 `mix-blend-mode:overlay` で入れたら、明るい地の上で粒子がほぼ消えた。** overlay は下地が明るいほど結果が白へ寄るため。**彩度を落とした灰色の粒子をそのまま薄く重ねる**方式に変えて解決（実測で確認）。

**② 「ヘッダー、フッダー、ロゴ横に『CARSHOP Pazoo』と入れてCARSHOPは小さめにして」**
- `.brandtxt` を新設し、ヘッダーとフッターの**ロゴの右**に置いた。`CAR SHOP`＝11px・字間 .22em、`Pazoo`＝27px。書体は Barlow Condensed。
- 色＝ヘッダーは `Pazoo` ワイン／`CAR SHOP` グレー、フッターは `#efece5`／`#8d867a`。
- **900px以下ではヘッダーの文字を隠す**（LINE・電話・3本線と競って詰まるため）。**フッターには常に出る**。

**③ 「ヘッダーロゴもっと大きくできない」**
- ヘッダーのロゴ **44 → 62px**（スクロールで詰まった時 32 → 42px、スマホ 34 → 44px）。ヘッダーの高さは 72 → **91px**。
- 文字の `Pazoo` も 23 → 27px にして釣り合いを取った。

実測＝ヘッダー高さ91px・ロゴ128×62px（PC1440／1000とも）／JSエラー0／横スクロール0／390pxでもヘッダーが崩れないことを目視。Artifact 更新（版「ヴィンテージ質感・ロゴ横に店名・ロゴ拡大 9/5」）。

### 2026-09-05 さらに5件（社長）

| 指示 | 実装 |
|---|---|
| 🔴「ストック矢印押すと下の商品ページに飛んでスライドできない」 | **不具合。直した。** 矢印を `pointer-events:none` の飾りにしていたため、押すと下のカードのリンク（グーネット）へ抜けていた。→ `<span>` を **`<button>`** に変え、押すと `rail.scrollBy({left:386})` で次へ送る。消えている時（右端）は押せない。hover と focus の見た目も追加 |
| 「事故にあわれたら タイトルの横に入れて文字サイズタイトルより小さく」 | `.h2 .ja` を `display:block`（下の行）→ **`display:inline`（英字の横）**。`clamp(13px,1.6vw,21px)`。**帯の中に入る**ので色を変更＝明るい面 `rgba(255,255,255,.88)`／暗い面 `rgba(33,31,27,.82)`。560px以下は下の行に戻し、色も元に戻す。★INSURANCE だけでなく**全セクションに適用**（1つだけだと揃わないため） |
| 在庫の説明文（「全車ではない。一部なのでお店にも来てみてください的な文に」／「文字サイズもう少し大きく」／「色はあずき色」） | 文を差し替え＝**「ここに出ているのは在庫の一部です。…お店に来ていただければ、いまある車をぜんぶご覧いただけます。」**／`#stock .lead` を **18px・ワイン `#7d2a3d`（あずき色）** に |
| 「01 — Order こも文字サイズ小さすぎる大きくして」 | `.cell .no` `.ledger .no` を **19 → 26px**（13→19→26） |
| 「CAR SHOPもう少し大きく」 | `.brandtxt small` を **11 → 14px**（字間 .22→.2em、太さ600→700） |

🔴 **和文を横に置いた時の位置合わせ＝実測で詰めた。**
`vertical-align:baseline` のままだと**帯の下端に張り付いて切れて見えた**（実測：帯 -8〜82／和文 51〜81）。`vertical-align:1.25em` にして **和文 26〜56**（帯の中心37に対して和文の中心41）。狭い幅でも確認（744px＝帯 -4〜48／和文 13〜32・中心22で一致）。

実測＝矢印は `tagName=BUTTON`・`pointer-events:auto`・押すと `scrollBy left=386` が呼ばれ L0→386（**外部ページへは飛ばない**）／`#stock .lead` = 18px `rgb(125,42,61)`／`.cell .no` `.ledger .no` = 26px／JSエラー0／横スクロール0。Artifact 更新（版「矢印をボタンに・和文を見出し横・在庫文を変更 9/5」）。

### 2026-09-05 さらに3件（社長）

| 指示 | 実装 |
|---|---|
| 「お問い合わせ電話番号とLINEお問い合わせボタンは常に表示」 | **900px以下に、画面下の固定バー**を新設（`.fixbar`）。左＝「お電話 0248-76-2077」／右＝緑の「LINEで相談」。`body{padding-bottom:66px}` でフッターが隠れないようにし、3本線メニューを開いている間は出さない。★**PCはヘッダーが `position:sticky` で貼りついたまま**なので固定バーは出さない（実測で確認済み） |
| 「ヘッダーロゴ 店名小さいな」／「CAR SHOPもう少し大きく」 | `.brandtxt` の `Pazoo` **27 → 34px**、`CAR SHOP` **14 → 17px**。スクロールで詰まった時も 19 → 26px |
| 「買う。探す。直す。守る。などの小タイトルが帯の中央 ミントの時は黒字」 | `.h2` を **`display:flex; align-items:center`** に変え、英字と和文を**縦の中央で揃える**。ミントの帯の上は **`var(--dark)` の黒字** |

🔴 **`vertical-align` の数値合わせをやめた。** 1440pxでは合っても他の幅でズレるため（実測：帯の中心37に対し和文の中心41）。flex の中央揃えにしたら **1440px＝帯中心37／和文中心38、884px＝26／27** とどの幅でも揃った。

🔴 **「ミントの時は黒字」が効かなかった原因。** 下の方に残っていた `.dark .h2 .ja,.dark .lead{color:#b3ada2}` が、同じ強さで**後ろから**打ち消していた。`.dark .lead` だけに分けて解消（実測：`rgb(179,173,162)` → `rgb(33,31,27)`）。

実測＝`#service` 和文 `rgb(33,31,27)`／`#faq` 和文 `rgba(255,255,255,.88)`／`.brandtxt` 34px／`.fixbar` は1440pxで `none`・700pxで `grid`／JSエラー0／横スクロール0。Artifact 更新（版「連絡バー常時表示・小タイトル中央・店名拡大 9/5」）。

### 2026-09-05 説明文を機械的でない書き方に直した（社長「みため自然に、機械的に見えないようにしてくれ 全ての項目」）

**機械的に見えていた原因（3つ）**
1. **同じ長さの文が同じ語尾で並ぶ**。全部「〜です。」「〜ます。」で、1文の長さもほぼ同じ。
2. **「〜のこと、〜のこと、〜のこと」型の3つ並べ**（連絡の章）。AI臭の典型。
3. **固有名詞と数字が無い**章がある。

**直し方**（`hp-seisaku` スキルの「文章の型を外す」に沿う）
- 1文の長さをわざとばらす。**言い切りの短文**（「よそで買った車も同じです。」）や**体言止めを1つだけ**（「国道4号沿い、須賀川インターから5分。」）挟んで、ですます調の連続を切る。
- 3つ並べをやめる。「下取りの相場でも、車検の時期の相談でも構いません。」のように2つに減らして口語にする。
- **各章に固有名詞か数字を1つ入れる**＝在庫「国道4号沿い・須賀川インターから5分」／連絡「10時から19時まで、火曜だけ休み」／声「接客の評価 4.9」。
- 話し言葉に寄せる（「案内されたけど、そこに出さないとダメなの？」／「ぜんぶ同じ店で。」）。

| 章 | 直したこと |
|---|---|
| トップ直下 | 「好きな店です」→「**好きでやっている店です**」。列挙の締めを「ぜんぶ同じ店で。」と体言止めにして、最後に場所を1文で |
| 在庫 | 5文フラット → 長短を混ぜ、途中に「国道4号沿い、須賀川インターから5分。」を挟む |
| サービス | 2文 → 「よそで買った車も同じです。」を足して3文目を短く言い切る（FAQの内容と一致） |
| 保険 | 引用を口語に。「——これがいちばん多いご質問です。」で受ける |
| 声 | 「口コミより（接客評価 4.9）」→「口コミから、**そのまま載せています**。接客の評価は 4.9 です。」 |
| 連絡 | 「〜のこと」3つ並べを廃止。営業時間を入れて具体にした |
| フッター | トップと同文だったので、短い別バージョンに |

実測＝機械的な型の残り＝「〜のこと×3」0件／「お届けします・安心信頼・丁寧に」0件／JSエラー0／横スクロール0／表示崩れなし。Artifact 更新（版「説明文を機械的でない書き方に 9/5」）。

### 2026-09-05 お客様の声にグーネットの実口コミを載せた（社長「GOOのお客様の声から拾えないかな、かなり評価いいんだよ」）

**道具**＝`tools/goo-reviews.mjs`（新規）。`https://www.goo-net.com/user_review/0902905/detail.html` を生HTMLで取り、EUC-JPを復号して1件ずつ拾う。冒頭しか出ていない口コミは詳細ページから全文を取り直す。出力＝`data/reviews_latest.json` ／ `data/口コミ_latest.md`。

🔴 **「接客評価 4.9」は誤りだった。** 実際の項目は **レビュー総合 5.0／説明のわかりやすさ 5.0／納車までの対応 5.0／おすすめ度 5.0／お問い合わせ（見積り）4.9**。4.9は「お問い合わせ（見積り）」の点で、接客の点ではない。**社長に見せる前に自分の書いた数字が間違っていた**ので、正しい5項目に差し替えた。

**載せたもの**（口コミは**10件**あった）
- 点数の並び5項目。**同じ大きさで5つ並べず、総合だけ56px**にして崩した（AI臭の「同サイズのカード並べ」回避）。
- 引用**4件**（原文のまま・手を入れない）＝ジムニー2件（タカハシさん2019年6月／MASSAOさん2018年5月）、ハイラックスサーフ（イチサムさん2018年7月）、アクア（ミッチーサッチーさん2018年4月）。
- 出典＝「2026-09-05 時点で10件。上は4件の抜粋です。」＋グーネットの全件ページへのリンク。
- `要取得` の印は消えた（実データが入ったため）。

🟡 **本番公開の前に店主様へ確認すること**＝グーネットの口コミを自社サイトへ転載してよいか。ニックネームは公開の投稿名だが、念のため確認する。

実測＝引用4件・点数5項目が表示／`todo` の残り11件（他章の要確認。声の分は消えた）／JSエラー0／横スクロール0。Artifact 更新（版「お客様の声にグーの実口コミを掲載 9/5」）。

**2026-09-05 社長決定：グーネットの口コミ掲載は OK。** 店主様への確認は不要（社長判断）。転載の可否について確認シートへ足す件は取り下げ。

### 2026-09-05 B（GBP整備）→ A（HP側）（社長「BA進めて」）

**A＝HP側・実装して公開した**
- `<head>` に **AutoDealer の JSON-LD**（名称・住所・電話・FAX・営業時間＝火曜を除く6日・親会社・サービス7件・sameAs）。JSONの構文とNAPは実測で検証。
- 会社概要の下に **Googleマップの埋め込み**（住所ベース・APIキー不要）＋「Googleマップで開く」「ここへの経路を調べる」。GBPの直リンクは**URL未確認**なので `要確認` の印つき。
- ★**Artifact は外部サイトの iframe を読み込めない**ので、`build_pazoo.js` の**手順2.5**で iframe を住所の板に自動で差し替える。**本番用の `pazoo.html` は iframe のまま**。

🔴 **星（`aggregateRating` / `review`）は意図的に入れていない。**
①Googleは2019年に自社サイトの自社評価（self-serving review）をリッチリザルトから外した＝**入れても星は出ない**。②**よそから集めた口コミのマークアップは規約違反**。→ HPに出している口コミ4件は「読み物」であって構造化データにはしない。**この線引きを崩さないこと。**

**B＝GBP整備案を1本にまとめた**＝`GBP_整備案_2026-09-05.md`
基本情報（NAPをHPと一致）／カテゴリ（主＝中古車販売店、副4つ）／説明文の下書き約400字／写真の割り当て／商品・サービス（**価格が分からないものは空のまま**）／属性／Q&A5件／口コミ運用／やらないこと。

🔴 **着手前に社長の確認が要る3点**＝GBPが①存在するか ②オーナー確認済みか ③口コミ何件か。**オーナー確認は店主様本人でないとできない**（代行不可）。
🔴 **GBPでいちばん効くのは「人」の写真。** 確認シート⑩Aが未着なので、優先して撮っていただきたい。

実測＝JSON-LD構文OK・`@type=AutoDealer`・星なし／地図の iframe が表示・導線リンク2本／配布版では住所の板に差し替わることをビルドログで確認／JSエラー0／横スクロール0。Artifact 更新（版「構造化データ・地図・Google導線を追加 9/5」）。

### 2026-09-05 ヴィンテージ感を強めた（社長「背景ヴィンテージ感があるのはどうなった？」→「もっと強く出して」）
9/5 に入れたものは**薄すぎて気づけない濃さ**だった。指示を受けて上げた。

| | 前 | 後 |
|---|---|---|
| 紙の粒子（フィルム粒状・220pxタイル） | opacity .085 | **.19** |
| 周辺の落ち（ビネット） | `rgba(33,31,27,.11)`・56%から | **`.24`・44%から**（内側から効き始める） |
| **紙のムラ（新規）** | なし | **900pxタイルの低周波ノイズを重ねた**（`baseFrequency 0.012`・3オクターブ）。雲のような濃淡＝紙の焼け |

- 粒子とムラは `body::after` の**背景2枚重ね**（`background-size:220px 220px,900px 900px`）。`body::before` がビネット。どちらも `position:fixed` ＋ `pointer-events:none`。
- **色は足していない**（灰色の透明度だけ）。ただし濃くしたぶん、クリームは少しくすむ。これ以上上げると「古びた」より「汚れた」に寄るので、上げるならここが上限に近い。
- 画像ファイルは増えていない（データURI）。配布版は 1.74MB のまま。

実測＝`body::after` の背景4層（url×2＋タイル指定）・opacity 0.19／`body::before` は radial-gradient／JSエラー0／横スクロール0。加工の有無を同じ位置で撮り比べて、**違いが目で分かることを確認**。Artifact 更新（版「ヴィンテージ感を強めた 9/5」）。

### 2026-09-05 ヴィンテージ加工は取りやめ（社長「ヴィンテージ感なしでいいです」）
- `body::before`（周辺の落ち）と `body::after`（粒子＋紙のムラ）を**削除**。写真も色もそのまま、素の状態に戻した。
- **戻したいときのために `demo/pazoo.html.bak_vintage` に加工入りを残してある**（粒子 .19／ビネット .24／900pxのムラ）。
- 同日の「全背景 少しヴィンテージ感だして」「もっと強く出して」は**この決定で取り消し**。

実測＝`body::after` `body::before` とも `content:none`＝出ていない／JSエラー0／横スクロール0。Artifact 更新（版「ヴィンテージ加工を外した 9/5」）。

### 2026-09-05 お客様の声をPOPに／Instagramを追加（社長）

**「ここもっと伝わるように、POPにアイコンなど使って」**
- **アイコンはSVGで作って `<symbol>` に定義**（星・引用符・車・Instagram）。**絵文字は使わない**（AI臭8点の「装飾の絵文字」に当たるため）。星は評価という意味を持つので装飾ではない。
- **点数の帯**＝白地の箱に。左に **5.0 を76px＋★5つ**、右に残り4項目を「★＋数字＋項目名」の2列で。4.9は**半分の星**（塗りなし・線だけ）で表す。
- **口コミはカード**に。**1件目だけ横いっぱいの大判**（文字21px）、残り3件を3列。**同じ大きさで4枚並べない**＝AI臭の「同サイズのカード」回避。
- カードの中身＝薄い引用符アイコン／本文／区切り線／**頭文字のアバター**（ワインの丸）＋名前＋**車のアイコン付きの車種**＋★5つ＋投稿月。
- 星の色はワイン。ミントは明るすぎてクリーム地で沈むため使わない。

**「一応インスタもいれるかな」**
- お問い合わせを **LINE／電話／Instagram の3枠**に（`2.4fr 1.8fr 1.8fr`＝等分にしない）。`@carshoppazoo` へ実リンク。
- 🟡 **フィードの埋め込みはしていない。** Behold.so 等を使うには**顧客のFeed IDが必要**（`hp-seisaku` の標準部品）。いまはリンクのみ。フィードを出すならIDを取得してから。

実測＝カード4枚・星45個／お問い合わせ3枠・列幅 `448px 336px 336px`／JSエラー0／横スクロール0。Artifact 更新（版「お客様の声をカードに・Instagram追加 9/5」）。

### 2026-09-05 Instagramのフィード枠を作った（社長「やる」）
- **Instagram セクションを新設**（お問い合わせと会社概要の間）。正方形8枚・PC4列／スマホ2列。ドロワーとフッターMenuにも導線を追加。
- 読み込みは **Behold.so**（`https://feeds.behold.so/<Feed ID>`）。実装は `assets/js/site.js`（2020年版）と同じ形を `demo/pazoo.html` に持ってきた。
- **待ち箇所＝`var BEHOLD_FEED_ID = '';`**。IDが入るまでは「Instagram連携 未設定」の板が出るだけで、**空のまま公開しても崩れない**。
- 手順書＝**`Instagram連携_手順案内_LINE用.md`**（J-DRYの型を流用。LINEにコピペする文面つき）。

🔴 **Artifactでは投稿が並ばない**（外部への通信ができないため）。**本番のサイトでは並ぶ。**
🔴 **Instagramがプロアカウント（ビジネス／クリエイター）でないとBeholdに繋がらない。** `@carshoppazoo` がどちらかは**要確認**。ここで止まる案件が多い。

🟡 **踏んだ小バグ：`@carshoppazoo` が `@CARSHOPPAZOO` と大文字になっていた。** `.h2{text-transform:uppercase}` が和文の添え字にもかかっていたため。`.h2 .ja{text-transform:none}` を足して解消（実測：`textTransform` が `none` に）。

実測＝セクション高さ572px・待ちの板あり／添え字 `@carshoppazoo`（小文字）／JSエラー0／横スクロール0。Artifact 更新（版「Instagramフィードの枠を新設 9/5」）。

### 2026-09-05 店名の表記（社長「店名 Pazoo（パズー）→ CARSHOP Pazoo にして」）
- 会社概要の店名を **「CAR SHOP Pazoo（カーショップ パズー）」** に変更。
- ★**「CARSHOP」ではなく「CAR SHOP」（スペースあり）にそろえた。** ロゴの図案が `CAR SHOP` と分かち書きで、ヘッダー／フッターの `.brandtxt`・構造化データの `name`・地図の title もすべて `CAR SHOP Pazoo`。**NAP統一のため表記は1つに固定する**（GBPも同じ表記で登録する＝`GBP_整備案_2026-09-05.md` §1）。
- 読みの「カーショップ パズー」は括弧で残した。構造化データの `alternateName` と一致する。

実測＝店名セル `CAR SHOP Pazoo（カーショップ パズー）`／サイト内の表示箇所4か所すべて `CAR SHOP Pazoo` で統一（`CARSHOP` の残りはCSSコメント内の引用のみ）／JSエラー0／横スクロール0。Artifact 更新（版「店名をCAR SHOP Pazooに 9/5」）。

### 2026-09-05 背景に薄い山なみ（社長「背景に薄くアウトドアっぽい山や海の画像いれれますか？」）
まず**権利の心配がない方法**で1つ作って見てもらう形にした。

- **写真ではなく、自分で描いたSVGの山なみ**を `.intro`（FV直下の帯）の下側に敷いた。切り抜きジムニーの後ろに山が来る。
- 色は**クリーム `#f4f0e8` と罫線 `#ddd5c5` の中間だけ**（`#e9e3d5` と `#e0d9c8` の2層）。**新しい色は足していない。**
- データURIなので**画像ファイルは増えない**し、拡大しても荒れない。720px以下は高さ180pxに落として密度を下げる。

**写真でやる場合の道は3つ**（社長に選んでもらう）
1. 🥇 **社長・店主様が撮った写真**。ジムニーで出かけた先の山や海があれば最良。**実物である価値**が出るし、権利の問題が一切ない。
2. **フリー素材**（Unsplash / Pexels 等の商用可ライセンス）。すぐ入るが、**よそのサイトと写真がかぶる**。「AIで作った感」が出やすいのはこの道。
3. **いまの描き起こし**（今回入れたもの）。かぶらない・軽い・権利の心配ゼロ。ただし写真ほどの空気感は出ない。

🔴 **拾ってきた写真をそのまま使うことはしない。** 出所の分からない画像を顧客のサイトに載せるのは事故のもと。

実測＝`.intro` の背景に data URI のSVG・`background-size:100% 260px`／JSエラー0／横スクロール0。Artifact 更新（版「背景に薄い山なみ（描き起こし） 9/5」）。

### 2026-09-05 山と海を他の章にも（社長「こんな感じでいいよ。ミントの薄い色など使って海や山も他項目の背景に入れてみて」）
描き起こしのSVGで統一。**写真は使っていない**（権利の心配なし・ファイルも増えない）。

| 章 | 風景 | 濃さ |
|---|---|---|
| トップ直下（`.intro`） | **山** | クリーム系 `#d8d1bf` ＋ **ミント22%** を1層混ぜた |
| サービス（`#service`・暗い面） | **山** | ミント 7%／12%。暗い地に浮くくらいの薄さ |
| お客様の声（`#voice`） | **海（波）** | ミント 13%／20% |
| お問い合わせ（`#contact`） | **海（波）** | ミント 10%／17%。声より控えめにして繰り返し感を消す |

**入れなかった章＝在庫・事故にあわれたら・FAQ・会社概要。**
- 在庫はカードが主役、会社概要は地図があるので背景を足すと散らかる。
- **「事故にあわれたら」は内容が重い**ので、あえて静かなままにした。
- ★**全部の章に敷かない。** 山→（空ける）→山→（空ける）→海→海 と切り替えることでリズムが出る。全面に敷くと壁紙になって効かない。

実測＝`#service` `#voice` `#contact` の3つとも `background-image` にデータURIが入っている／`.intro` も同様／JSエラー0／横スクロール0／暗い面で山が、明るい面で波が見えることを目視。Artifact 更新（版「山と海の背景を4か所に 9/5」）。

### 2026-09-05 訂正：風景は「トップ下の山だけ・ミントなし」（社長「最初の感じでいいです。トップ下だけミントなしの山で」）
- **サービス／お客様の声／お問い合わせの背景を撤去。**
- **トップ直下（`.intro`）の山だけ**を残し、色は**最初に作ったクリーム系の2色（`#e9e3d5`／`#e0d9c8`）**に戻した。**ミントは使わない。**
- 同日の「ミントの薄い色など使って海や山も他項目の背景に」は**この決定で取り消し**（記録として前節に残す）。

実測＝背景の有無＝`.intro` あり／`#service` `#voice` `#contact` すべて `none`／ミントの風景の残り0件／JSエラー0／横スクロール0。配布版 1.76→1.75MB。Artifact 更新（版「トップ下の山だけに戻した 9/5」）。

---

## 📌 2026-09-05 引継ぎを作成
**`引継ぎ_2026-09-05.md` が最新の引継ぎ。** `引継ぎ_2026-09-02.md` の先頭には「これは古い」と案内を入れた。
このファイル（デザイン確定）は**指示と実装の履歴の正本**として、引き続き末尾に追記していく。取りやめた案も消さずに残す。
