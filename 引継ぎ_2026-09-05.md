# Pazoo HP 引継ぎ（2026-09-05）

> 🔴**この文書は 2026-09-09 で失効した。`引継ぎ_2026-09-09.md` を見ること。**
> 「直すのは `demo/pazoo.html` だけ」は誤り＝9月デザインを本番9ページへ実装したため、直すのは `index.html` と下層8ページ。


前の引継ぎ＝`引継ぎ_2026-09-02.md`。**この2日で大きく変わったので、以後はこちらを見る。**

---

## 0. まず知っておくこと

- **この案件は「実績作り」ではない。** 現行 https://pazoo.co.jp （WordPress）は**当社が2020年頃に制作した既存客**で、そのリニューアル。受注前デモの台帳（`_shared/デモ公開台帳.md`）の対象外。
- 参考サイト＝**Renoca**（https://www.flexnet.co.jp/renoca/ ）。社長が5件から選定。
- **社長は毎日この案を見て、細かく指示を出してくる。** 指示は必ず `デザイン確定_2026-09-02.md` の末尾に追記して残す（取りやめた案も記録として残す）。

## 1. どこに何があるか

| | |
|---|---|
| **直すのはここだけ** | `demo/pazoo.html`（1141行・1枚もの） |
| 配布用（自動生成・**直接編集しない**） | `demo/artifact_pazoo.html`（1.75MB） |
| 生成コマンド | `node C:/HQ/projects/pazoo-hp/demo/tools/build_pazoo.js` |
| **Artifact URL** | https://claude.ai/code/artifact/9713878a-f2c6-4b50-aa17-b7c0c57b4558 |
| 手元で見る | `demo/★Pazoo案を見る.bat` |
| デザインの正本 | `デザイン確定_2026-09-02.md`（**末尾に9/4〜9/5の追記が大量にある**） |
| トップ写真の比較 | `demo/候補_トップ写真.html` |

★**Artifact はログインしていないと開けない。** 社長が「PCで見れない」と言ったときは、claude.ai へのログインを確認するか、`.bat` でローカルを開く。

### build_pazoo.js が配布版でやる差し替え（3つ）
1. `assets/img/main/*.webp` を **参照されているものだけ** データURIに（決め打ちしない）
2. ロゴを データURIに
3. **地図の iframe を住所の板に**（Artifact は外部サイトの iframe を読めないため）。**本番用の `pazoo.html` は iframe のまま**

## 2. いまの見た目（9/5 時点）

- **トップ写真5枚**＝JB64フロント（店の看板入り）→ 店舗外観 → 展示車 → 待合 → 仕上がったお車。5秒ごと。
- **FV直下の帯**＝クリーム地に**切り抜いた赤ジムニー**。右外から回り込んで入る（1.6s）。スマホは見出しの右へ絶対配置。**背景に薄い山**（自作SVG・ミントなし）。
- **大見出し7つ**＝画面の端から端まで届く帯。明るい面＝ワインの帯＋白文字／暗い面（SERVICE・INSURANCE）＝**ミントの帯＋黒文字**。和文の添え字は英字の**横**、帯の**中央**。
- **在庫**＝グーネットの実在庫6台。ジムニー3台を先頭に固定。右端に**押せる「→」ボタン**（点滅＋横に動く／右端まで送ると消える）。
- **お客様の声**＝グーネットの実口コミ。点数5項目（総合5.0）＋カード4枚（1件目だけ大判）。星・引用符・車・Instagram のアイコンは**全部SVGで自作**（絵文字は使わない）。
- **Instagram**＝セクションだけ用意。**Feed ID 待ち**。
- **フッター**＝ぶんちゃん式の4列（ブランド／Menu／Service／Shop）＋下段。ロゴは**台紙なしの透過**。
- **スマホだけ画面下に固定バー**（お電話／LINEで相談）。PCはヘッダーが `position:sticky` なので出さない。
- **構造化データ**＝`AutoDealer` の JSON-LD。★**星（aggregateRating／review）は入れない**（§6）。

## 3. 動いている自動化

| | |
|---|---|
| **グーネット在庫を毎日1回** | `tools/goo-stock.mjs`。`nightly.ps1` の**手順5.5**（毎晩21時）で走る |
| 出し先 | **けんしろうの21時LINEに合流**。★**変わった日だけ**1行が出る |
| サイトへの反映 | `tools/apply-stock.mjs --write` → `build_pazoo.js` → Artifact（**手作業**） |
| 口コミの取得 | `tools/goo-reviews.mjs`（手で実行） |
| 出力 | `data/stock_latest.json` `data/在庫_latest.md` `data/reviews_latest.json` `data/口コミ_latest.md` |
| 手順の正本 | `手順_グーネット在庫の日次確認.md` |

🔴 **在庫は最終更新から48時間で表示が止まる安全弁がある。** 9/4に実際に止まっていた。**在庫を触ったら必ず `apply-stock.mjs --write` で日付を進める。**
★**Artifact への反映は自動化できない**（`claude -p` にArtifactツールが渡らない）。

## 4. 待っているもの（社長・店主様へ）

**確認シート（`確認シート_LINE用_2026-09-02.md`）で頼んでいるもの**
- ⑨ **店主のひとこと** — 未着。FV直下に枠だけ出ている
- ⑩A **人の写真**（店主様かスタッフ）— **未着。これがいちばん効く。** HPにもGBPにも同じものを使う
- ⑪ 数字1つ（板金の目安価格など）— 未着

**ページに出ている `要確認` 13か所**
LINE公式アカウントのURL／Googleビジネスプロフィールのリンク／Instagram の Feed ID／切り抜きの車種名・年式／コーティングのブランド・料金／レンタカーの有無／スタッフ人数／取扱信販／代車が無料か／出張査定・必要書類

**GBP（`GBP_整備案_2026-09-05.md`）**
★着手前に社長が確認する3点＝①GBPが存在するか ②オーナー確認済みか ③口コミ何件か。**オーナー確認は店主様本人でないとできない。**

**Instagram（`Instagram連携_手順案内_LINE用.md`）**
LINEに送る文面つき。★**プロアカウント（ビジネス／クリエイター）でないとBeholdに繋がらない。**

## 5. 次にやること（優先順）

1. **人の写真をもらう**（⑩A）。HP・GBP両方に効く
2. **GBPの状態を確認 → §0の3点** → `GBP_整備案` を投入
3. **Instagram の Feed ID をもらう** → `demo/pazoo.html` の `var BEHOLD_FEED_ID = '';` に入れるだけ
4. 店主のひとこと（⑨）が届いたら、原文のまま差し替え
5. 本番公開（**エックスサーバー**）。その前に `kansa-irai`（`CHECKLIST_KOKAI.md`）＋ `secret-scan`
6. 本番公開時に直すもの＝在庫写真を自社サーバーへ移す／プライバシーポリシーのリンク（いま `../privacy.html`）

## 6. 🔴 触る前に読む（実測で踏んだ罠）

**構造化データ・SEO**
- **星（`aggregateRating`／`review`）を入れてはいけない。** ①Googleは2019年に自社サイトの自社評価をリッチリザルトから外した＝入れても星は出ない ②**よそから集めた口コミのマークアップは規約違反**。グーネットの口コミは「読み物」として載せるだけ。
- GBPとHPの**NAP（名称・住所・電話）は一字一句そろえる**。店名は **`CAR SHOP Pazoo`**（スペースあり）で固定。

**スクレイピング**
- **要約AIに読ませない。** WebFetchは「ジムニー」を「ダイハツ ムーヴ」、75万円を「1,350万円」と読んだ。**必ず生HTMLをパースする。**
- **グーネットは EUC-JP。** `res.text()` では全部化ける。`new TextDecoder("euc-jp")` を使う。
- 値段は**価格セルの中だけ**で探す（外まで探すとローンの月々を拾う）。表のセルには `<br/>` が入る。

**CSS・JS**
- `rail.scrollLeft += 0.45` は**永久に進まない**（小数が整数に丸められる）。位置はJS側の変数で持って代入する。
- `mix-blend-mode:overlay` は**明るい地の上で粒子がほぼ消える**。
- `left:50%;width:100vw;margin-left:-50vw` の全幅は、**その要素の中心が画面の中心でないと端まで届かない**（`.stock-head` の flex で踏んだ）。
- `vertical-align` の数値合わせは**画面幅でズレる**。中央に置きたいなら flex の `align-items:center`。
- **後ろに同じ強さの指定が残っていると打ち消される**（`.dark .h2 .ja` で2回踏んだ）。色が効かないときはまず**下**を見る。
- `.h2{text-transform:uppercase}` は**和文の添え字にもかかる**（`@carshoppazoo` が大文字化した）。
- 飾りに見えても**押せるようにする**。`pointer-events:none` の矢印は下のリンクに抜けて商品ページへ飛んだ。

**Chromeヘッドレスでの検証**
- **`requestAnimationFrame` が数フレームで止まる。** 動きを測るなら rAF を `setTimeout` に差し替える。
- **`scrollLeft` を代入しても `scroll` イベントが出ないことがある。** `dispatchEvent(new Event("scroll"))` で確かめる。
- **最小ウィンドウ幅が500px。** 390pxを見たいときは iframe に入れて撮る。
- **Artifact は外部への通信ができない**（地図の iframe・Beholdのfetch）。**本番では動く。**

**その他**
- **社長と同時にPhotoshop／Illustratorは使えない。** 走らせる前に `Get-Process Photoshop,Illustrator` で確認する。
- **ヒアドキュメントは `\` を食う**。`\t` が化ける。CSS・JSを書き換えるときは Edit ツールを使う。

## 7. 取りやめた案（記録・戻せる）

| 案 | どうなったか | 戻し方 |
|---|---|---|
| 在庫の自動スライド（130px/秒・継ぎ目なし） | 社長「無し」 | `デザイン確定` 9/5 の該当節に仕様あり |
| ヴィンテージ加工（粒子・紙のムラ・周辺の落ち） | 社長「なしでいい」 | **`demo/pazoo.html.bak_vintage` に加工入りが残っている** |
| ミントの山・海を他の章にも | 社長「トップ下だけミントなしの山で」 | `デザイン確定` 9/5 の該当節に色と数値あり |
| SERVICE・INSURANCE の見出しをミント文字 | 帯に置き換えた | — |
| トップ写真に赤ジムニー | 帯の切り抜きと同じ車なので外した | 素材は `素材/追加写真_2026-09-04/01_…` |
